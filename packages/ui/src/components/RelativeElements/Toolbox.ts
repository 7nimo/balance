import styled from 'styled-components';

export const Toolbox = styled.div`
  position: absolute;
  bottom: 40px;
  left: 40px;
  font-size: 24px;
  z-index: 3;
  display: grid;
  place-items: center;
  background: rgba(87,120, 120, .3);
  padding: 8px;
  border-radius: 5px;
`;
